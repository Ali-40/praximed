# PraxisMed — backend runtime dependencies
# Sprint 14 / Module 101 — Railway Backend Deployment Prep
# Python 3.11 required (see runtime.txt at repo root)
#
# Direct runtime dependencies only. Development/test tools (httpx, pytest,
# pytest-asyncio, anyio) are not listed here — they are not needed to run the
# backend on Railway.
#
# Transitive dependencies (SQLAlchemy, Mako for Alembic; uvloop, httptools for
# uvicorn[standard]) are resolved automatically by pip.

fastapi==0.138.2
uvicorn[standard]==0.49.0
asyncpg==0.31.0
alembic==1.18.5
pydantic==2.13.4
PyJWT==2.4.0
bcrypt==3.2.0
