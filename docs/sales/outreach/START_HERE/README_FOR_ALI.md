# Tomorrow — do this

1. Open `2_TODAY_CALL_LIST.xlsx`
2. Call the top 10 (work down the list)
3. Use `3_EMAIL_DRAFTS.md` if they ask for an email
4. Update `4_MASTER_TRACKER.xlsx` after every call
5. Stop coding, start calling
