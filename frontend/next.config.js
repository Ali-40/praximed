/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_API_BASE_URL is read at build time from the environment.
  // Set it in .env.local for local development.
}

module.exports = nextConfig
